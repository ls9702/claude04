# Xcode
*.xcodeproj/
xcuserdata/
*.xcuserstate
DerivedData/
build/
.build/
*.swp
.DS_Store
# SwiftPM
.swiftpm/
Package.resolved
# 로컬 전용
scripts/device.local
.env.local
.claude/settings.local.json
# XcodeGen이 project.yml의 info.properties로 생성 (커밋하지 않음)
TripShot/Info.plist
# Core ML 컴파일 결과(Xcode가 .mlpackage에서 빌드 시 생성). 원본 .mlpackage만 커밋한다.
*.mlmodelc
